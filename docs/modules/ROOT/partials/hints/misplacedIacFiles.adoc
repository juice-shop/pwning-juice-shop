* The developers probably used some kind of tool to automate their cloud deployment.
* Such tools typically store their configuration in a directory with a very predictable name.
* Try common directory names that popular deployment automation tools use by convention.
